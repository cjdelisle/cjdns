[workspace]

members = [
    "rust/cjdns_sys",
    "rust/cjdroute",
    "rust/testcjdroute",
]

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
panic = "unwind"

# In debug mode, enable optimizations for dependencies, but not for our code
[profile.dev.package."*"]
opt-level = 3
